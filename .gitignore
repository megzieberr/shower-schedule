# Dependencies and build output (never committed)
node_modules
dist

# Local environment files — these may contain values you do not want in the repo
.env
.env.local
.env.*.local

# Supabase CLI working files
supabase/.temp
supabase/.branches

# Misc
.DS_Store
*.log
